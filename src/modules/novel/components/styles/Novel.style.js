import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    flex: 1,
    flexDirection: 'column',
    alignItems: 'center'
  },
  box: {
    width: 100,
    height: 200,
    backgroundColor: '#fff',
    marginTop: 20
  }
})
